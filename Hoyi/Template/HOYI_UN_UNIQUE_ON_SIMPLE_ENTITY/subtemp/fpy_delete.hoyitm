if (true)  // 如果启用了首字母存储，没有的话，请删除这里的代码.
					{
						<%%# e.ClassName #%%>_fpy.E.Where(<%%# e.ClassName #%%>.<%%# e.getfirfieldName() #%%> == <%%# e.getfirfieldName() #%%>).Delete();
                    }